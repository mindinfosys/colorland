<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record model="ir.ui.view" id="custom_view_stock_quant_form">
        <field name="name">stock.quant.custom.form</field>
        <field name="model">stock.quant</field>
        <field eval="10" name="priority"/>
        <field name="arch" type="xml">
            <form string="Inventory Valuation" create="false" edit="false" delete="false">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" icon="fa-exchange" type="object" name="action_view_stock_moves" string="Product Moves"/>
                    </div>
                    <group>
                        <group>
                            <field name="product_id"/>
                            <field name="location_id" options="{'no_create': True}"/>
                            <field name="lot_id" groups="stock.group_production_lot"/>
                            <field name="package_id" groups="stock.group_tracking_lot"/>
                            <field name="owner_id" groups="stock.group_tracking_owner"/>
                        </group>
                        <group>
                            <label for="quantity" string="Quantity On Hand"/>
                            <div class="o_row">
                                <field name="quantity"/>
                                <field name="product_uom_id" groups="uom.group_uom"/>
                            </div>
                            <label for="reserved_quantity" string="Quantity Reserved"/>
                            <div class="o_row">
                                <field name="reserved_quantity"/>
                                <field name="product_uom_id" groups="uom.group_uom"/>
                            </div>
                            <field name="in_date" attrs="{'invisible': [('lot_id', '=', False)]}" groups="stock.group_production_lot"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="custom_view_stock_quant_tree_editable">
        <field name="name">stock.quant.custom.tree.editable</field>
        <field name="model">stock.quant</field>
        <field eval="10" name="priority"/>
        <field name="arch" type="xml">
            <tree decoration-danger="inventory_quantity &lt; 0" editable="bottom"
                  create="1" edit="1" import="0" js_class="singleton_list">
                <field name="custom_barcode"/>
                <field name="custom_sale_price"/>
                <field name="custom_cost_price"/>
                <field name="id" invisible="1"/>
                <field name="tracking" invisible="1"/>
                <field name="product_id" attrs="{'readonly': [('id', '!=', False)]}"
                       invisible="context.get('single_product', False)"
                       readonly="context.get('single_product', False)" force_save="1"
                       options="{'no_create': True}"/>
                <field name="location_id" attrs="{'readonly': [('id', '!=', False)]}"
                       invisible="context.get('hide_location', False)"
                       options="{'no_create': True}"/>
                <field name="lot_id" groups="stock.group_production_lot"
                       attrs="{
                            'readonly': ['|', ('id', '!=', False), ('tracking', 'not in', ['serial', 'lot'])],
                            'required': [('tracking', '!=', 'none')]
                       }"
                       invisible="context.get('hide_lot', False)"
                       context="{'default_product_id': product_id, 'default_company_id': company_id}"/>
                <field name="package_id" groups="stock.group_tracking_lot"
                       attrs="{'readonly': [('id', '!=', False)]}"/>
                <field name="owner_id" groups="stock.group_tracking_owner"
                       attrs="{'readonly': [('id', '!=', False)]}"
                       options="{'no_create': True}"/>
                <field name="quantity" string="On Hand Quantity"
                       invisible="1"/>
                <field name="inventory_quantity" string="On Hand Quantity"
                       readonly="0"/>
                <field name="reserved_quantity" optional="hide"/>
                <field name="product_uom_id" groups="uom.group_uom"/>
                <field name='company_id' groups="base.group_multi_company" optional="show"/>
            </tree>
        </field>
    </record>

    <record model="ir.ui.view" id="custom_view_stock_quant_tree">
        <field name="name">stock.quant.custom.tree</field>
        <field name="model">stock.quant</field>
        <field eval="10" name="priority"/>
        <field name="arch" type="xml">
            <tree string="Inventory Valuation" js_class="inventory_report_list"
                  create="0" delete="0" decoration-danger="quantity &lt; 0">
                <field name="product_id" invisible="context.get('single_product', False)"/>
                <field name="location_id" invisible="context.get('hide_location', False)"/>
                <field name="lot_id" groups="stock.group_production_lot"
                       invisible="context.get('hide_lot', False)"/>
                <field name="package_id" groups="stock.group_tracking_lot"/>
                <field name="owner_id" groups="stock.group_tracking_owner"/>
                <field name="reserved_quantity" string="Reserved"/>
                <field name="quantity"/>
                <field name="product_uom_id" groups="uom.group_uom"/>
                <field name='company_id' groups="base.group_multi_company"/>
            </tree>
        </field>
    </record>

    <record model="ir.ui.view" id="custom_view_stock_quant_pivot">
        <field name="name">stock.quant.custom.pivot</field>
        <field name="model">stock.quant</field>
        <field name="arch" type="xml">
            <pivot string="Inventory">
                <field name="product_id" type="row"/>
                <field name="location_id" type="col"/>
                <field name="quantity" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="custom_stock_quant_view_graph" model="ir.ui.view">
        <field name="name">stock.quant.custom.graph</field>
        <field name="model">stock.quant</field>
        <field name="arch" type="xml">
            <graph string="Stock Quant">
                <field name="location_id"/>
                <field name="quantity" type="measure"/>
            </graph>
        </field>
    </record>

    <record id="custom_action_view_quants_new" model="ir.actions.act_window">
        <field name="name">Inventory Multicompany Quants</field>
        <field name="res_model">stock.quant</field>
        <field name="view_mode">tree,form,pivot,graph</field>
        <field name="context">{'custom_inventory_company_ids': True,
                'search_default_internal_loc':1,
                'search_default_productgroup':1,
                'search_default_locationgroup':1,
                'inventory_mode': True,}</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'tree', 'view_id': ref('stock_request_multicomapny.custom_view_stock_quant_tree_editable')}),
            (0, 0, {'view_mode': 'pivot', 'view_id': ref('stock_request_multicomapny.custom_view_stock_quant_pivot')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('stock_request_multicomapny.custom_view_stock_quant_form')}),
            (0, 0, {'view_mode': 'graph', 'view_id': ref('stock_request_multicomapny.custom_stock_quant_view_graph')})]"/>
        <field name="domain"></field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create Quants
          </p>
        </field>
    </record>

    <menuitem id="menu_inventory_multicompany" name="Inventory Multicompany Report" action="custom_action_view_quants_new" parent="stock.menu_warehouse_report"/>

</odoo>

